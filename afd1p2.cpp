#include <bits/stdc++.h>

using namespace std;

int main() {
    string a; cin>>a;
    string sol="";
    sol.push_back(a[3]);
    sol.push_back(a[2]);
    sol.push_back(a[7]);
    sol.push_back(a[0]);
    sol.push_back(a[5]);
    sol.push_back(a[1]);
    sol.push_back(a[4]);
    sol.push_back(a[6]);
    cout<<sol<<endl;
}